apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: parladata-lendava
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-production"
    nginx.ingress.kubernetes.io/proxy-body-size: 10m
spec:
  tls:
    - hosts:
        - parladata-lendava.lb.djnd.si
        - files.parladata-lendava.lb.djnd.si
      secretName: parladata-tls
  rules:
    - host: parladata-lendava.lb.djnd.si
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: parladata-lendava
                port:
                  number: 8000
    - host: files.parladata-lendava.lb.djnd.si
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: parladata-nginx
                port:
                  number: 80
