apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: parlasolr-ajdovscina
  labels:
    app: parlasolr-ajdovscina
spec:
  serviceName: parlasolr-ajdovscina
  replicas: 1
  selector:
    matchLabels:
      app: parlasolr-ajdovscina
  template:
    metadata:
      labels:
        app: parlasolr-ajdovscina
    spec:
      securityContext:
        fsGroup: 8983
      containers:
      - name: parlasolr-ajdovscina
        image: parlasolr-ajdovscina # kustomize thing
        volumeMounts:
        - name: parlasolr-ajdovscina-persistent-storage
          mountPath: /opt/solr/server/solr/mycores
        ports:
        - name: http
          containerPort: 8983
          protocol: TCP
        livenessProbe:
          tcpSocket:
            port: http
          initialDelaySeconds: 10
          periodSeconds: 15
        readinessProbe:
          httpGet:
            path: /solr/parlasearch/admin/ping
            port: http
          initialDelaySeconds: 15
          periodSeconds: 30
          timeoutSeconds: 5
        resources:
          requests:
            memory: 1Gi
            cpu: 500m
          limits:
            memory: 1Gi
            cpu: 500m
      initContainers:
      - name: create-core
        image: parlasolr-ajdovscina
        # command: ['solr', 'create_core', '-c', 'parlasearch']
        # create parlasearch core with a convenience script
        command: ['sh', '-c', '/opt/docker-solr/scripts/precreate-core parlasearch']
        volumeMounts:
        - name: parlasolr-ajdovscina-persistent-storage
          mountPath: /opt/solr/server/solr/mycores
        resources:
          requests:
            memory: 1Gi
            cpu: 500m
          limits:
            memory: 1Gi
            cpu: 500m
      - name: link-config-1
        image: parlasolr-ajdovscina
        command: ['cp', '-f', '/parlasearch-conf/schema.xml', '/opt/solr/server/solr/mycores/parlasearch/conf/']
        volumeMounts:
        - name: parlasolr-ajdovscina-persistent-storage
          mountPath: /opt/solr/server/solr/mycores
        resources:
          requests:
            memory: 200Mi
            cpu: 100m
          limits:
            memory: 200Mi
            cpu: 100m
      - name: link-config-2
        image: parlasolr-ajdovscina
        command: ['cp', '-f', '/parlasearch-conf/solrconfig.xml', '/opt/solr/server/solr/mycores/parlasearch/conf/']
        volumeMounts:
        - name: parlasolr-ajdovscina-persistent-storage
          mountPath: /opt/solr/server/solr/mycores
        resources:
          requests:
            memory: 200Mi
            cpu: 100m
          limits:
            memory: 200Mi
            cpu: 100m
  volumeClaimTemplates:
  - metadata:
      name: parlasolr-ajdovscina-persistent-storage
      annotations:
          pv.beta.kubernetes.io/gid: "8983"
    spec:
      storageClassName: 'scw-bssd-retain'
      accessModes:
        - ReadWriteOnce
      resources:
        requests:
          storage: 10Gi
---
apiVersion: v1
kind: Service
metadata:
  name: parlasolr-ajdovscina
spec:
  selector:
    app: parlasolr-ajdovscina
  ports:
    - protocol: TCP
      port: 8983
      targetPort: 8983
---
# This here ingress is for debugging purposes
# only. Make sure it's always commented out.
# Your SOLR instance should not accessible
# from the public internet.
# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: parlasolr
#   annotations:
#     nginx.ingress.kubernetes.io/rewrite-target: /
# spec:
#   rules:
#   - host: parlasolr.lb.djnd.si
#     http:
#       paths:
#       - path: /
#         pathType: Prefix
#         backend:
#           service:
#             name: parlasolr
#             port:
#               number: 8983
